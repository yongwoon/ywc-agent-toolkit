# Task Dependency Graph

<!-- Phase heading format: `## Phase <initials>-NNNNNN` — the resolved collaborator
     initials, a hyphen, then the 6-digit PHASE. Legacy unprefixed headings
     (`## Phase NNNNNN`) generated before initials namespacing remain valid and are
     never renumbered; both forms may coexist in this file. -->

## Phase yk-000001 — [Phase Name]
- `yk-000001-010-db-create-users-table` → (root)
- `yk-000001-020-domain-user-entity` → depends on `yk-000001-010`
- `yk-000001-030-api-user-registration` → depends on `yk-000001-020`

## Phase yk-000002 — [Phase Name]
- `yk-000002-010-lib-setup-redis` → (root)
- `yk-000002-020-domain-session-management` → depends on `yk-000001-020`, `yk-000002-010`

## Parallel Execution Notes
- Initial ready set: `yk-000001-010-db-create-users-table`, `yk-000002-010-lib-setup-redis`
- After `yk-000001-010-db-create-users-table` merges: `yk-000001-020-domain-user-entity` becomes runnable
- `yk-000001-020-domain-user-entity` and `yk-000002-020-domain-session-management` must not run together if they both modify `shared/types/**`

## Visual Dependency Graph (Optional)

Task 수가 5개 이상이거나 cross-phase dependency가 복잡한 경우, Mermaid diagram을 추가하면 가독성이 크게 향상됩니다.

```mermaid
graph LR
  subgraph Phase yk-000001
    A[yk-000001-010-db-create-users-table] --> B[yk-000001-020-domain-user-entity]
    B --> C[yk-000001-030-api-user-registration]
  end
  subgraph Phase yk-000002
    D[yk-000002-010-lib-setup-redis] --> E[yk-000002-020-domain-session-management]
    B --> E
  end
```

<!-- This graph serves as the single source of truth for execution order.
     Must be consistent with individual README.md "Dependencies" sections.
     The Mermaid diagram is optional but recommended for complex dependency structures.
     No special markup is needed for compaction: the shared completion marker
     runs claude-code/skills/ywc-task-generator/scripts/compact-dependency-graph.py
     after every task completion, reading the Phase / Parallel Execution Notes /
     Visual Dependency Graph headings directly. The script matches both the prefixed
     `## Phase <initials>-NNNNNN` form and the legacy unprefixed `## Phase NNNNNN`
     form, grouping on the full prefixed key so two collaborators' identically
     numbered phases never merge. Once every task id under a Phase heading exists in
     <tasks-dir>/completed/, that phase collapses to "## Phase <initials>-NNNNNN — done"
     and a "- Completed: ..." bullet; once every task id referenced by a Notes/Mermaid
     section is completed, that whole section is dropped. Do not hand-edit an
     already-compacted phase heading or bullet. -->
