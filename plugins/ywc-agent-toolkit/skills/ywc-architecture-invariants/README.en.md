# ywc-architecture-invariants

Validate an optional root `architecture-invariants.json` and bounded normalized
evidence. V1 is standard-library, validation-only, closed-schema, and launches no
child processes. An absent discovered manifest preserves existing workflows.
