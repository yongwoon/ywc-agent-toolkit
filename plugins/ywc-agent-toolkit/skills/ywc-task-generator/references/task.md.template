# [TASK_NAME] — Implementation Checklist

## Prerequisites
Verify these before starting:
- [ ] `[DEPENDENCY_TASK_A]` is completed (merged)
- [ ] `[DEPENDENCY_TASK_B]` is completed (merged)

## Allowed Edit Scope
- [ ] Stay within declared Ownership from `README.md`
- [ ] If the task requires edits outside Ownership, stop and report before proceeding

## Architecture Contract Packet
- [ ] Use only the packet in `README.md`; do not discover a second contract or infer edges locally
- [ ] Propagate `NEEDS_CONTEXT` for missing mapping, invalid evidence, or incomplete coverage
- [ ] Treat `VIOLATED` as a finding; do not escalate task-generator or implementer authority

## Stop Conditions
- [ ] Stop if a dependency task is not actually merged or its output contract is missing
- [ ] Stop if required changes spill into another task's Ownership
- [ ] Stop if a shared contract or schema must change beyond the declared Scope
- [ ] Stop if another in-flight task already owns the surface this task now needs to edit

## Implementation Steps
- [ ] [Specific step 1 — target: `src/models/user.ts`; e.g., "Create User entity definition"]
  - Related AC/FR: `[AC-1]` / `[FR-1]`
  - Contract / Behavior Change: `[externally visible or task-visible behavior affected]`
  - Verification Command / Evidence: `[task-specific command, test, diff review, or other concrete evidence]`
- [ ] [Specific step 2 — target: `src/users/validation.ts`; e.g., "Add validation logic for email field"]
  - Related AC/FR: `[AC-2]` / `[FR-2]`
  - Contract / Behavior Change: `[expected validation behavior or explicit no-public-contract-change note]`
  - Verification Command / Evidence: `[task-specific command, test, diff review, or other concrete evidence]`
- [ ] [Specific step 3]
  - Related AC/FR: `[AC/FR identifier]`
  - Contract / Behavior Change: `[expected contract or behavior change]`
  - Verification Command / Evidence: `[expected passing signal or evidence to collect]`
- ...

<!-- NOTE: A task is not ready if an implementation step cannot be traced to
     a spec outcome. Split, rewrite, or remove untraceable steps before saving
     the generated task. -->

## Task Verify
<!-- NOTE: For duplicate-sensitive writes such as stock, balance, quota,
     payment, order creation, or provisioning, include applicable verification
     for concurrent request behavior, rollback on partial failure, and
     idempotency retry / double-click behavior. Name the selected mechanism
     (atomic conditional update, row lock, optimistic lock, idempotency key,
     unique constraint, or persisted request/result record) and the expected
     observable result. For docs-only, config-only, or unrelated tasks, omit
     this note's checks. -->
- [ ] [Run task-specific verification command 1]
  - Expected Passing Signal: `[exit 0, named assertion passes, expected output appears, etc.]`
  - Pre-change Failing Evidence / Exception: `[RED evidence for bugfix/behavior tasks, or explicit exception reason]`
  - Contract/Test Evidence: `[contract test, integration test, snapshot, diff review, or N/A with reason]`
- [ ] [Run task-specific verification command 2]
  - Expected Passing Signal: `[exit 0, named assertion passes, expected output appears, etc.]`
  - Pre-change Failing Evidence / Exception: `[RED evidence for bugfix/behavior tasks, or explicit exception reason]`
  - Contract/Test Evidence: `[contract test, integration test, snapshot, diff review, or N/A with reason]`

## Verification
- [ ] lint passes (`[PROJECT_LINT_CMD]`)
- [ ] typecheck passes (`[PROJECT_TYPECHECK_CMD]`)
- [ ] unit tests pass (`[PROJECT_TEST_CMD]`)
- [ ] integration tests pass (if applicable)
- [ ] app builds without error (`[PROJECT_BUILD_CMD]`)

<!-- NOTE: Replace [PROJECT_*_CMD] placeholders with the actual commands
     collected during Step 2 (Collect Project Context).
     Examples: `pnpm lint`, `docker exec app npm run typecheck`, `make test` -->

<!-- NOTE: Implementation Steps must contain concrete, task-specific steps.
     Do NOT use generic placeholders like "implement core logic" or "handle edge cases".
     Each step should reference specific files, modules, functions, or behaviors. -->

<!-- NOTE: Implementation Steps depth depends on the selected Granularity Mode:
     - `human` mode: 10–20 fine-grained checkboxes
     - `llm` mode: 3–7 top-level bullets, each with 2–5 sub-bullets
     See references/granularity-modes.md for full rules. -->
