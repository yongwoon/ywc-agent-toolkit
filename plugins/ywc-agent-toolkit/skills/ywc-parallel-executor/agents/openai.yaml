interface:
  display_name: "Parallel Executor"
  short_description: "Run task waves with merge delivery"
  default_prompt: "Use $ywc-parallel-executor to execute independent task folders concurrently in isolated worktrees with wave gating, delivery mode handling, and cleanup."
