interface:
  display_name: "Sequential Executor"
  short_description: "Execute generated tasks end to end and preserve implementation notes"
  default_prompt: "Use $ywc-sequential-executor to resolve the next ready task or requested task range, execute each task through implementation and verification, preserve non-obvious implementation notes, preserve --pr-lang en|ja|ko|zh|es, and route resume, conflict, CI, URL-policy, and handoff transitions through bounded statuses without prompts."
