interface:
  display_name: "Plan"
  short_description: "Turn rough requests into execution plans"
  default_prompt: "Use $ywc-plan to clarify this change request, assess scale, and route it to the right specification, task, or implementation path."
