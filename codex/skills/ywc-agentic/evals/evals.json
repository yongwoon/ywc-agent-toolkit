{
  "skill_name": "ywc-agentic",
  "evals": [
    {
      "id": 1,
      "prompt": "Use agentic mode to implement user profile image uploads end to end. Allow at most two iterations.",
      "expected_output": "Receives the natural-language goal, decides Full or Resume mode, and creates a fixed spec path through ywc-plan. For Medium or Large work, runs ywc-spec-validate and ywc-task-generator, then chooses the sequential or parallel executor. Records the pre-iteration SHA before each iteration. The Evaluate phase runs ywc-impl-review against the original spec and the pre-iteration SHA..HEAD git range. If HIGH or CRITICAL findings remain, it does not raise max-iterations; it either re-plans or emits a ceiling report. Appends an iteration record to tasks/agentic-log.md and exits with Completion Status.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Use $ywc-agentic --resume --tasks-dir tasks/ to continue the existing task run.",
      "expected_output": "Detects Resume mode first. If tasks/ is empty or has no pending tasks, it does not silently restart in Full mode; it reports the mismatch and asks for user confirmation. If pending tasks exist, it skips the Plan phase and resumes from the Executor phase using the existing tasks/ directory and dependency graph. It does not re-run tasks already under completed/.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Only update the copy on a small settings screen. Use agentic and handle it automatically.",
      "expected_output": "When ywc-plan returns a Small verdict, uses the Small Path. Does not invoke ywc-task-generator, ywc-sequential-executor, or ywc-parallel-executor; instead runs ywc-code-gen directly. Still preserves pre-iteration SHA recording, ywc-impl-review evaluation, loop control, agentic-log, and Completion Report rules.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Run agentic mode for a medium feature. The implementation review returns DONE_WITH_CONCERNS with one correctness concern and one observation.",
      "expected_output": "Treats the correctness concern as blocking the iteration handoff and routes it back into the implementation loop before marking the iteration complete. Carries only observation-level concerns into the final report. Does not proceed to the next iteration or claim DONE until the correctness concern is fixed and re-reviewed.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Use $ywc-agentic --goal \"Implement account export\" --pr-lang zh --executor sequential.",
      "expected_output": "Accepts `--pr-lang zh` as a supported PR language and forwards it unchanged to the selected executor as `--pr-lang zh`. It does not pass task-generation `--lang` unless the user explicitly requested task/spec language or shared YWC language policy resolves one.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "Use $ywc-agentic --goal \"Generate Spanish task docs for billing retries\" --pr-lang es.",
      "expected_output": "Preserves `--pr-lang es` unchanged when invoking the executor. Because the user explicitly requested Spanish task docs, the Task Phase may pass `--lang es` to `ywc-task-generator`; absent that explicit task/spec language request or a shared YWC language policy resolution, it would not pass `--lang`.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "Use $ywc-agentic for a medium feature that must go through preview approval before task generation.",
      "expected_output": "The Medium/Large Task Phase uses exactly two ywc-task-generator calls with the same --spec path: first --preview-only with a persisted preview path, then --approve-preview --non-interactive with matching preview_path, preview_revision, and preview_digest. It logs the preview evidence in UTC before the approved consume call and treats stale, missing, mismatched, or bypassed preview identity as NEEDS_CONTEXT.",
      "files": []
    },
    {
      "id": 8,
      "prompt": "Use $ywc-agentic --non-interactive for a small feature when ywc-plan returns one valid agentic Result with a date-prefixed Artifact.",
      "expected_output": "Accepts exactly one labelled Result with Status: DONE, Scale: Small, and Artifact under docs/ywc-plans/YYYYMMDD-small_<slug>.md. Passes that exact Artifact to ywc-code-gen --spec and never constructs --output, plan.md, a basename, or a path from logs.",
      "files": []
    },
    {
      "id": 9,
      "prompt": "Use $ywc-agentic when ywc-plan returns duplicate Result blocks, a missing Scale, an out-of-root Artifact, or only an unlabelled prose path.",
      "expected_output": "Returns bounded BLOCKED naming only the producer, failed field, candidate count, path digest, and reason. It invokes zero downstream skills and does not persist or expose raw response/tool output.",
      "files": []
    },
    {
      "id": 10,
      "prompt": "Use $ywc-agentic --non-interactive for Medium work with a valid planner candidate, but omit --mode before task generation.",
      "expected_output": "Runs the planner and ywc-spec-ready authority checks only as applicable, then returns NEEDS_CONTEXT: --mode immediately before task generation with zero task-generator or executor calls and no prompt.",
      "files": []
    },
    {
      "id": 11,
      "prompt": "Use $ywc-agentic --non-interactive for a Medium feature where ywc-spec-ready reports Suggestions, then test --suggestions apply with residual Suggestions and --suggestions defer.",
      "expected_output": "Invokes the callee as `ywc-spec-ready --spec <verified-candidate> --non-interactive --suggestions apply|defer`, forwarding --non-interactive on every call and adding --suggestions only once preflight determines Suggestions exist; it never omits --non-interactive, never substitutes --output or a reconstructed path for the verified candidate, and never invents a --suggestions value. apply performs exactly one amendment/re-validation and returns NEEDS_CONTEXT: --suggestions with only the remaining count when Suggestions remain; defer records deferral and allows only a DONE ready Artifact. Missing --suggestions when Suggestions are reported returns bounded NEEDS_CONTEXT: --suggestions with zero downstream calls. A DONE Result from the callee advances to the Task Phase using that ready Artifact verbatim; any NEEDS_CONTEXT or other terminal status is propagated unchanged and stops before ywc-task-generator. No branch waits for user input.",
      "files": []
    },
    {
      "id": 12,
      "prompt": "Resume an agentic run whose selected executor has an authoritative checkpoint, without --resume-disposition, and with an invalid externalSpecUrls profile.",
      "expected_output": "Returns bounded NEEDS_CONTEXT for --resume-disposition or .codex/settings.local.json:ywDevSequentialExecutor.externalSpecUrls before executor invocation. It never prompts, mutates checkpoint state, guesses a URL policy, or calls downstream execution.",
      "files": []
    },
    {
      "id": 13,
      "name": "context-safety-routing-matrix",
      "prompt": "Run the focused agentic context-safety matrix with a valid Small Result, invalid Result variants, Medium/Large ready artifacts, missing --mode/--lang/--suggestions/--resume-disposition, and guessed paths.",
      "input": {"valid": "One labelled producer Result and an existing repository-relative Markdown Artifact.", "invalid": ["duplicate Result", "missing field", "out-of-root path", "basename reconstructed from logs", "missing deterministic non-interactive argument at its own staged preflight (e.g. --mode immediately before Medium/Large task generation, --suggestions after the ywc-spec-ready Result reports Suggestions, --resume-disposition after an executor checkpoint is found)"]},
      "expected_status": ["DONE", "BLOCKED", "NEEDS_CONTEXT"],
      "expected_downstream_calls": {"valid": 1, "invalid": 0, "note": "invalid counts calls at or after the rejecting stage's preflight; callees that legitimately completed at earlier stages are not counted — a Small-scale goal never reaches the --mode preflight at all"},
      "expected_handoff": "accepted only for the complete validated Result; rejected for every malformed, guessed, or incomplete input",
      "expected_output": "Uses only the paired labelled Scale/Artifact or final ready Artifact, returns the named bounded terminal status for each case, and never prompts or calls a downstream skill after a rejected preflight. Each argument is validated immediately before the callee that consumes it, so a Small-scale --non-interactive goal is never blocked on a missing --mode."
    }
  ]
}
