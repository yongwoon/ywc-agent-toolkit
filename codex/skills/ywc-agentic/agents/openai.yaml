interface:
  display_name: "Agentic"
  short_description: "Orchestrate ywc skills from goal to code"
  default_prompt: "Use $ywc-agentic to turn this high-level goal into a planned, implemented, verified ywc workflow, forwarding explicit task/spec language only when requested and preserving --pr-lang en|ja|ko|zh|es to executors."
