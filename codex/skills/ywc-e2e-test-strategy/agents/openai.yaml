interface:
  display_name: "E2E Test Strategy"
  short_description: "Design Playwright E2E strategy and CI"
  default_prompt: "Use $ywc-e2e-test-strategy to design Playwright coverage for critical flows and wire it into CI when needed."
