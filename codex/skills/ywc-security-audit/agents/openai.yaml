interface:
  display_name: "Security Audit"
  short_description: "reviewing authentication/authorization code, external-facing endpoints, code handling sensitive data, or for periodic security..."
  default_prompt: "Use $ywc-security-audit to handle this request following the skill workflow, loading bundled references or scripts only when needed."
