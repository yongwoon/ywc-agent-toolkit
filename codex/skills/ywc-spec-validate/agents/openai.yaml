interface:
  display_name: "Spec Validate"
  short_description: "Use after writing a specification and before task decomposition, when the user wants to validate spec completeness, consistency..."
  default_prompt: "Use $ywc-spec-validate to handle this request following the skill workflow, loading bundled references or scripts only when needed."
