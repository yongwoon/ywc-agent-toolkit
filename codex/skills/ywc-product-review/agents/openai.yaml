interface:
  display_name: "Product Review"
  short_description: "the user wants a business/service perspective review of a project (user value, UX, growth, risk, market). Triggers: \"product re..."
  default_prompt: "Use $ywc-product-review to handle this request following the skill workflow, loading bundled references or scripts only when needed."
