interface:
  display_name: "Spec Writer"
  short_description: "Update project specs from tasks or PRs"
  default_prompt: "Use $ywc-spec-writer to create or update docs/specification from project context, task ranges, commits, or PR diffs."
