interface:
  display_name: "Spec Writer"
  short_description: "Create or update project specifications from tasks, commits, or PRs"
  default_prompt: "Use $ywc-spec-writer to create or update docs/specification with clear feature, data, and workflow details, including task ranges, commit refs, or PR diffs when provided."
