# Task Dependency Graph

Preview Approval Metadata (when preview flow is used):
- `Preview Path:` `docs/ywc-plans/<slug>.task-preview.md`
- `Preview Revision:` `rev-001`
- `Preview Digest:` `sha256:...`
- `Spec Path:` `docs/ywc-plans/<slug>.md`
- `Tasks Dir:` `tasks/`
- `Mode:` `human` or `llm`

## Phase yk-000001 — [Phase Name]
- `yk-000001-010-db-create-users-table` → (root)
- `yk-000001-020-domain-user-entity` → depends on `yk-000001-010` | Refactor Phase: `phase-1` | Batch ID: `user-core-a`
- `yk-000001-030-api-user-registration` → depends on `yk-000001-020`

## Phase yk-000002 — [Phase Name]
- `yk-000002-010-lib-setup-redis` → (root)
- `yk-000002-020-domain-session-management` → depends on `yk-000001-020`, `yk-000002-010` | Refactor Phase: `phase-2` | Batch ID: `session-core-a`

## Parallel Execution Notes
- Initial ready set: `yk-000001-010-db-create-users-table`, `yk-000002-010-lib-setup-redis`
- After `yk-000001-010-db-create-users-table` merges: `yk-000001-020-domain-user-entity` becomes runnable
- `yk-000001-020-domain-user-entity` and `yk-000002-020-domain-session-management` must not run together if they both modify `shared/types/**`

## Visual Dependency Graph (Optional)

Task 수가 5개 이상이거나 cross-phase dependency가 복잡한 경우, Mermaid diagram을 추가하면 가독성이 크게 향상됩니다.

```mermaid
graph LR
  subgraph Phase 000001
    A[000001-010-db-create-users-table] --> B[000001-020-domain-user-entity]
    B --> C[000001-030-api-user-registration]
  end
  subgraph Phase 000002
    D[000002-010-lib-setup-redis] --> E[000002-020-domain-session-management]
    B --> E
  end
```

<!-- This graph serves as the single source of truth for execution order.
     Must be consistent with individual README.md "Dependencies" sections.
     The Mermaid diagram is optional but recommended for complex dependency structures.
     No special markup is needed for compaction: the shared completion marker
     runs codex/skills/ywc-task-generator/scripts/compact-dependency-graph.py
     after every task completion, reading the Phase / Parallel Execution Notes /
     Visual Dependency Graph headings directly. Once every task id under a Phase
     heading exists in <tasks-dir>/completed/, that phase collapses to
     "## Phase NNNNNN — done" and a "- Completed: ..." bullet; once every task
     id referenced by a Notes/Mermaid section is completed, that whole section
     is dropped. Do not hand-edit an already-compacted phase heading or bullet. -->
