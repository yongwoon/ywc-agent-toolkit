# yw-000036-020-domain-parallel-executor-codex-state-schema

## Purpose
Apply the identical mechanical field addition from `yw-000036-010` to the codex copy of the state-mutation script, keeping the two files in the byte-near-identical state the repo relies on.

## Scope
- `codex/skills/scripts/update-state.py`: same `has_contract` read + `integration_branch` seed in `cmd_init_parallel`, same `hardener-verdict` / `promotion-retry` subcommands, mirroring `yw-000036-010`'s exact field names and shapes.
- No other file.

## Spec Reference

### Primary Sources
- `docs/ywc-plans/20260910-parallel-executor-hardener-delivery-isolation.md#fr-5-update-statepy--one-new-field-both-copies` — FR-5: "Apply the identical edit to `claude-code/skills/scripts/update-state.py` and `codex/skills/scripts/update-state.py` (byte-identical today but for one docstring line)"
- `docs/ywc-plans/20260910-parallel-executor-hardener-delivery-isolation.md#acceptance-criteria` — AC7: `diff claude-code/skills/scripts/update-state.py codex/skills/scripts/update-state.py` must still report only the pre-existing one-line docstring difference
- `claude-code/skills/CLAUDE.md` §"Codex-skill: Maintained Independently" — this is a separate edit to a separate file, not a shared file, even though the content is identical

### Summary
`codex/skills/scripts/update-state.py` is byte-identical to the claude-code copy except one docstring line (verified by `diff` in the spec's investigation). This task re-applies the exact three-field addition and two-subcommand addition `yw-000036-010` made, so the post-edit `diff` between the two files still shows only that pre-existing docstring difference — never a `gate_state` divergence, a differently-named subcommand, or a differently-shaped seed condition.

### Out of Scope (from spec)
- Any codex-idiom rewording of the Python code itself — this is a script, not skill prose; there is no "codex idiom" for Python logic, only for the SKILL.md prose (`yw-000036-040`).
- The claude-code copy — already done by `yw-000036-010`.

## Criticality
`normal` — local JSON state-file schema addition, no auth/payment/secret/PII surface.

## Dependencies

### Depends On
- `yw-000036-010-domain-parallel-executor-state-schema` — provides the exact field names, subcommand names, and seed logic this task mirrors; also the byte-diff baseline this task's Task Verify checks against.

### Depended By
- `yw-000036-040-domain-parallel-executor-hardener-isolation-codex` — codex SKILL.md prose cites the subcommand names/args this task adds.

## Key Files
- `codex/skills/scripts/update-state.py` — identical edit to `yw-000036-010`'s `claude-code/skills/scripts/update-state.py` changes.

## Notes
- Copy the exact subcommand names chosen in `yw-000036-010` (do not independently invent different names) — AC7's diff check will fail otherwise.
- If `yw-000036-010` chose a different branch-name form than `wave-int/<N>` (see its Notes on the still-open naming question), mirror that choice here rather than defaulting independently.
- `plugins/ywc-agent-toolkit/skills/` is regenerated by `.githooks/pre-commit` + `scripts/sync-codex-plugin.sh` from `codex/skills` — do not hand-edit anything under `plugins/`.

## Parallel Execution Metadata

### Ownership
- `codex/skills/scripts/update-state.py`

### Owned Interface
- Same as `yw-000036-010`'s Owned Interface, mirrored onto the codex file: `integration_branch: str | None` seeding, `hardener-verdict N VERDICT`, `promotion-retry N`.

### Shared Surfaces
- `.ywc-run-state.json` schema (shared with the claude-code copy's consumers — in practice a project runs one executor at a time, but the schema itself is the shared contract).
- `plugins/ywc-agent-toolkit/skills/` — read-only from this task's perspective; regenerated by the sync hook, never hand-edited.

### Conflicts With
- (None identified — disjoint file from `yw-000036-010`)

### Parallelizable After
- `yw-000036-010-domain-parallel-executor-state-schema`

### Task Verify
- Same functional checks as `yw-000036-010`'s Task Verify, run against `codex/skills/scripts/update-state.py` instead.
- `diff claude-code/skills/scripts/update-state.py codex/skills/scripts/update-state.py` — confirm the only reported difference is the pre-existing single docstring line (no new divergence introduced by this task).
- `bash scripts/validate.sh`

## Out of Scope
- The claude-code file (`yw-000036-010`).
- Regenerating or hand-editing `plugins/ywc-agent-toolkit/skills/` — the pre-commit hook does this.
