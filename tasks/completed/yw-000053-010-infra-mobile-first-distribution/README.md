# yw-000053-010-infra-mobile-first-distribution

## Purpose
Synchronize the Codex plugin package and complete source/generated parity validation for the mobile-first contract.

## Scope
Run source-first deterministic checks, synchronize `codex/skills/` into the generated plugin mirror, inspect the generated diff, and run whitespace and full validation checks.

## Spec Reference

### Primary Sources
- `docs/ywc-plans/20260918-codex-mobile-first-ui-default.md#functional-requirements`
- `docs/ywc-plans/20260918-codex-mobile-first-ui-default.md#verification`

### Summary
Codex source files are authoritative and the plugin mirror is generated by `scripts/sync-codex-plugin.sh`. This task verifies the source contract and agent evals before synchronization, then validates generated parity and repository structure. It must not hand-edit generated plugin content.

### Out of Scope (from spec)
- Source policy, consumer, reviewer, or evaluator implementation — handled by preceding tasks.
- Claude Code changes, existing CSS rewrites, breakpoint-value prescriptions, and new agent roles.

## Criticality
normal — package synchronization and validation only.

## Dependencies

### Depends On
- `yw-000052-010-test-mobile-first-contract-eval` — provides passing deterministic source-contract evidence.

### Depended By
- (None — final task in this batch.)

## Key Files
- `plugins/ywc-agent-toolkit/skills/**` — generated Codex plugin mirror.
- `plugins/ywc-agent-toolkit/.codex-plugin/plugin.json` — generated package manifest copy.

## Notes
Run source-level checks first. Do not hand-edit files under the generated plugin mirror.

## Hardening Evidence

### Test Feedback Path
- Existing coverage: `bash scripts/sync-codex-plugin.sh`, `git diff --check`, and `bash scripts/validate.sh`.
- Named exception: no production behavior; package parity and validation replace RED-first runtime tests.

### Interface Contract
- `(N/A — generated package mirror has no independent public interface.)`

### Critical Surface Review
- Review requirement: `N/A`

### Data Integrity Hardening
- Trigger surface: `N/A — generated/package validation`
- Atomic / locking strategy: `N/A`
- Transaction boundary: `N/A`
- Idempotency guard: `N/A`
- Required tests: `N/A`

## Parallel Execution Metadata

### Ownership
- `plugins/ywc-agent-toolkit/skills/**`
- `plugins/ywc-agent-toolkit/.codex-plugin/plugin.json`

### Shared Surfaces
- Generated mirror of `codex/skills/**`.
- Repository-wide validation output.

### Conflicts With
- Any task modifying `plugins/ywc-agent-toolkit/**` or running package synchronization concurrently.

### Parallelizable After
- `yw-000052-010-test-mobile-first-contract-eval`

### Task Verify
- `bash scripts/run-codex-skill-contract-evals.sh`
- `bash scripts/check-codex-agent-evals.sh`
- `bash scripts/sync-codex-plugin.sh`
- `git diff --check`
- `bash scripts/validate.sh`

## Out of Scope
Do not edit generated files manually, alter source policy semantics, modify Claude Code artifacts, or introduce new deployment/configuration behavior.
