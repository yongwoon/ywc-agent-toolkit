# 000062-010-infra-auth-implement-distribution-validation

## Purpose

완성된 source skill을 prescribed script로 marketplace plugin에 동기화하고 install·source·plugin validation evidence를 남긴다.

## Scope

- `bash scripts/sync-codex-plugin.sh`로 generated plugin을 갱신한다.
- eval, description, disposable install, full validation, diff whitespace를 검증한다.

## Spec Reference

### Primary Sources

- `docs/ywc-plans/codex_auth_implement_skill.md#acceptance-criteria` — AC10 distribution gate
- `docs/ywc-plans/codex_auth_implement_skill.md#verification` — prescribed validation commands
- `codex/AGENTS.md` — source/generated plugin ownership boundary

### Summary

이 task는 source files를 authoring하지 않는다. 모든 Phase 000061 source work가 merge된 뒤 sync script가 plugin artifact를 생성하며, disposable `CODEX_HOME` install과 repository validation이 final distribution evidence가 된다. 실패는 command/error evidence와 함께 보고한다.

### Out of Scope (from spec)

- source skill, eval, catalog/root README authoring — Phase `000061` tasks
- plugin file 직접 편집, release metadata edit, consumer application auth 구현 — feature 전체 범위 밖

## Dependencies

### Depends On

- `000061-010-domain-auth-implement-skill` — complete source skill artifacts
- `000061-020-test-auth-implement-routing-evals` — valid routing fixture
- `000061-030-docs-auth-implement-catalogs` — final source catalog and count records

### Depended By

- (None — final task)

## Key Files

- `plugins/ywc-agent-toolkit/skills/ywc-auth-implement/**` — generated by sync script
- `plugins/ywc-agent-toolkit/.codex-plugin/plugin.json` — copied by sync script when applicable

## Notes

- generated plugin paths are outputs of `scripts/sync-codex-plugin.sh`; never hand-edit them.
- disposable install uses a temporary `CODEX_HOME` and must confirm `SKILL.md` plus `agents/openai.yaml`.

## Hardening Evidence

### Test Feedback Path

- Existing coverage: `scripts/run-codex-skill-contract-evals.sh`, `scripts/validate.sh`, disposable installer

### Interface Contract

- Contract: source-to-plugin Codex skill package
- Inputs: complete `codex/skills/ywc-auth-implement/**`
- Outputs: identical generated skill under `plugins/ywc-agent-toolkit/skills/`
- Error model: sync/install/validation command nonzero exit blocks completion
- Impacted tests: `bash scripts/validate.sh`

### Critical Surface Review

- Review requirement: manual full implementation review — authentication orchestration distribution artifact

### Data Integrity Hardening

- Trigger surface: N/A — deterministic generated documentation package
- Atomic / locking strategy: N/A
- Transaction boundary: N/A
- Idempotency guard: sync script replaces generated directory deterministically
- Required tests: repeated sync remains clean under `git diff --check`

## Parallel Execution Metadata

### Ownership

Generated `plugins/ywc-agent-toolkit/**` only through `bash scripts/sync-codex-plugin.sh`; verification commands do not expand edit scope.

### Shared Surfaces

Entire Codex marketplace package and repository-wide validation contracts.

### Conflicts With

All Phase `000061` tasks until they merge; synchronization snapshots the complete source tree.

### Parallelizable After

All Phase `000061` tasks merged.

### Task Verify

- `python3 -m json.tool codex/skills/ywc-auth-implement/evals/evals.json >/dev/null`
- `bash scripts/check-codex-skill-descriptions.sh --paths a-m`
- `bash scripts/run-codex-skill-contract-evals.sh`
- `tmp_codex_home="$(mktemp -d)"; CODEX_HOME="$tmp_codex_home" bash scripts/install.sh --codex ywc-auth-implement; test -f "$tmp_codex_home/skills/ywc-auth-implement/SKILL.md"; test -f "$tmp_codex_home/skills/ywc-auth-implement/agents/openai.yaml"; rm -rf "$tmp_codex_home"`
- `bash scripts/sync-codex-plugin.sh && bash scripts/validate.sh && git diff --check`

## Out of Scope

Manual generated-package edits, source content changes, and release metadata changes.
