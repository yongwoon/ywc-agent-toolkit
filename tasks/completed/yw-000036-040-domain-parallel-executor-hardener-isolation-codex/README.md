# yw-000036-040-domain-parallel-executor-hardener-isolation-codex

## Purpose
Port `yw-000036-030`'s wave-integration-branch model to `codex/skills/ywc-parallel-executor/SKILL.md`, in codex's own idiom, and resolve the pre-existing numbering/placement contradiction at `4e.5` (its prose already says "Before delivery" while its number implies after-delivery).

## Scope
- `codex/skills/ywc-parallel-executor/SKILL.md`: apply the same model as `yw-000036-030` (Pre-flight scan, `wave-int/<N>` creation/idempotent-reuse/push-at-creation, mode-mapping `--base-branch`, promotion step, carve-out rule + `--per-task-pr` advisory cap, Step 4i third bucket) in codex's own idiom (no `model:` fields, generic worker terminology, matching prose language) — and fix the numbering/placement contradiction at `:282-283` per the architecture verdict (the prose is the correct intent; the numbering is the bug).
- New codex `references/` document mirroring `yw-000036-030`'s `wave-integration-branch.md`, written in this file's idiom.
- No claude-code file is touched by this task.

## Spec Reference

### Primary Sources
- `docs/ywc-plans/20260910-parallel-executor-hardener-delivery-isolation.md#fr-7-codex-variant--same-model-plus-resolving-the-numbering-contradiction` — FR-7 (apply FR-1–FR-4 in codex idiom, fix the numbering contradiction as a bug fix, not a preserved runtime divergence)
- `docs/ywc-plans/20260910-parallel-executor-hardener-delivery-isolation.md#fr-12-step-4i-terminal-state-audit--the-hardener-blocked-wave-bucket` — FR-12 (codex half)
- `docs/ywc-plans/20260910-parallel-executor-hardener-delivery-isolation.md#acceptance-criteria` — AC9 (numbering agreement), AC11 (plugin mirror regenerated, not hand-edited), AC12, AC14
- `docs/ywc-plans/20260910-parallel-executor-hardener-delivery-isolation.architecture-verdict.md` §"Codex variant" — "The codex prose ('Before delivery, aggregate…') is the correct intent; the numbering/placement is the bug."
- `codex/skills/ywc-parallel-executor/SKILL.md:282-283` (`4e.5`, contradiction site), `:220` (`4a`), `:282` (`4e`), `:401` (`4i`)
- `codex/skills/ywc-sequential-executor/references/aggregate-pr.md` (codex-idiom push-at-creation precedent — cite this file's idiom, not the claude-code file's, per Amendment Log Iteration 3 Critical #3)
- `yw-000036-030-domain-parallel-executor-hardener-isolation-claude` — the claude-code sibling this task mirrors the model from (not the prose)

### Summary
Codex's `4e.5` already states the correct intent ("Before delivery, aggregate all task and gate results at the wave boundary") but its heading sits after `4e`'s delivery body, and its own Hardener procedure (`ownership.test_fixture_paths`, sanitized bounded packets, `enforced`/`advisory`/`report-only` tiers) runs against the wave's merged diff exactly like claude-code's — meaning it inherits the identical too-late-to-block defect. This task applies the same wave-integration-branch fix, expressed in codex's structurally different vocabulary: `contract_state` instead of `gate_state`, its own three-tier enforcement model already fits the enforcement-eligibility rule directly (FR-8, `yw-000036-050`).

### Out of Scope (from spec)
- The claude-code file — `yw-000036-030`.
- Reconciling the two `quality-gates.md` files' broader divergence — parent-spec open question, not this task.
- `plugins/ywc-agent-toolkit/skills/` — regenerated by `.githooks/pre-commit`, never hand-edited by this task.

## Criticality
`normal` — skill prompt/reference text; no auth/payment/secret/PII surface.

## Dependencies

### Depends On
- `yw-000036-020-domain-parallel-executor-codex-state-schema` — provides the codex `integration_branch`/`hardener_verdict`/`promotion_retry_count` fields and subcommands this task's prose describes.
- `yw-000036-030-domain-parallel-executor-hardener-isolation-claude` — supplies the settled model (branch-name form, promotion-step semantics) this task ports; not a file dependency, but the model must exist before porting it.

### Depended By
- `yw-000036-060-docs-adr-hardener-isolation` — the ADR references both roots' final implementations.

## Key Files
- `codex/skills/ywc-parallel-executor/SKILL.md` — Pre-flight scan, `4e` amendment, corrected `4e.5` numbering/placement, new promotion step, carve-out rule, Step 4i third bucket.
- `codex/skills/ywc-parallel-executor/references/wave-integration-branch.md` (new, codex idiom).

## Notes
- Do not treat the numbering contradiction as a deliberate runtime divergence to preserve — it is a bug (per architecture verdict), fix it to agree with the corrected model.
- Mirror `yw-000036-030`'s decided promotion-step label and `wave-int/<N>` form for consistency across roots, but express every procedural statement in codex's existing three-tier (`report-only`/`advisory`/`enforced`) vocabulary rather than translating `gate_state` terms literally.
- Codex's `4c.5`/`4c.6` already implement fine-grained Ownership-rejection and retry-guard behavior around Cleaner/Hardener dispatch — this task's Pre-flight scan and promotion step must not duplicate or contradict that existing dispatch-eligibility logic, only add the wave-boundary branch-topology change around it.
- `codex/skills/ywc-parallel-executor/SKILL.md` is at 504 lines (over the soft cap) at investigation time — keep body growth minimal, push bulk content into the new reference file.

## Parallel Execution Metadata

### Ownership
- `codex/skills/ywc-parallel-executor/SKILL.md`
- `codex/skills/ywc-parallel-executor/references/wave-integration-branch.md` (new)

### Owned Interface
- (None — no public interface owned; mirrors `yw-000036-030`'s decided branch-name/step-label strings.)

### Shared Surfaces
- `codex/skills/scripts/update-state.py` (read-only — invoked via CLI, not edited)
- `codex/skills/references/quality-gates.md` (read-only link, not edited — that's `yw-000036-050`)
- `codex/skills/ywc-sequential-executor/references/aggregate-pr.md` (read-only precedent for push-at-creation idiom)
- `plugins/ywc-agent-toolkit/skills/` (read-only from this task — regenerated by the sync hook)

### Conflicts With
- (None identified — disjoint files from every other task in this batch)

### Parallelizable After
- `yw-000036-020-domain-parallel-executor-codex-state-schema`
- `yw-000036-030-domain-parallel-executor-hardener-isolation-claude` (model must be settled first)

### Task Verify
- `grep -n "^\*\*4[a-z]" codex/skills/ywc-parallel-executor/SKILL.md` — confirm the wave-boundary Hardener step's heading position and step number now agree with its prose, and no step's prose says "Before delivery" while its number implies after-delivery.
- `grep -n "wave-int/" codex/skills/ywc-parallel-executor/SKILL.md`
- `grep -c "isolation branch can only protect state whose point-of-no-return" codex/skills/ywc-parallel-executor/SKILL.md`
- `grep -n "Hardener-BLOCKED\|hardener.*blocked" codex/skills/ywc-parallel-executor/SKILL.md` — Step 4i third bucket.
- `bash scripts/validate.sh` exits 0 (AC11 — also confirms Codex frontmatter has only `name`/`description`, no Claude-only fields).
- `git diff --stat plugins/ywc-agent-toolkit/skills` after running the sync — shows changes only in files `scripts/sync-codex-plugin.sh` produces, never hand-edited.

## Out of Scope
- Any change to `claude-code/skills/ywc-parallel-executor/SKILL.md` or its references — `yw-000036-030`.
- Any change to `codex/skills/references/quality-gates.md` — `yw-000036-050`.
- Writing the ADR — `yw-000036-060`.
- Hand-editing `plugins/ywc-agent-toolkit/skills/`.
