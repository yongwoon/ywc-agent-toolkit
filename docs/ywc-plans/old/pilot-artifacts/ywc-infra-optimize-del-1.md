# Dispatch artifact: ywc-infra-optimize (deleted body, run 1)

**Key**: claude-code/skills/ywc-infra-optimize/SKILL.md:45-45
**Scenario**: I need help with the following: improving existing infrastructure: cost right-sizing, removing unused resources, reserved/spot adoption, drift detection & remediation, and reliability hardening for AWS/GCP/Azure/K8s — the safe change-loop equivalent of refactor-clean for infra.
**Variant**: deleted
**Refused/escalated**: True
**Refusal reason**: Not a refusal of the request itself — the request squarely matches this skill's stated purpose. The escalation is procedural: before Step 1 can run for real, I would need the user to confirm which repository/path actually holds the live Terraform-managed AWS/GCP/Azure/K8s infrastructure (this repo is a skill-distribution toolkit with no IaC tree), and I would need confirmation that cloud credentials / terraform backend access / kubeconfig context are available in the execution environment. Separately, per the skill's own tiering rules, CAUTION and DANGER candidates are always escalated to the user or to ywc-iac-author rather than executed, regardless of how confident the classification looks.

## Actions
- Read docs/ywc-plans/pilot-artifacts/variant-ywc-infra-optimize-45-45.md (ywc-infra-optimize procedure) as the sole instruction source
- Announce skill start using the exact required announcement line
- Check current repo for actual Terraform/IaC content (git ls-files '*.tf', find for *.tf/*.tfvars) before doing anything else, since ywc-agent-toolkit is a skill-distribution repo and likely has no infra to act on
- If no IaC found, ask the user which repository/path contains the live AWS/GCP/Azure/K8s Terraform state rather than assuming a scope
- Step 1: default --scope to full IaC tree; run terraform init and terraform plan -detailed-exitcode per Terraform root/workspace for drift detection; capture terraform show -json output
- Step 1: dispatch Task(subagent_type: ywc-performance-engineer) read-only, pointed at references/lenses/cost.md and references/finops.md, with the §3.5 return-payload contract injected from references/subagent-status-actions.md, to produce the cost/utilization report
- Read the returned cost/utilization report artifact file
- Step 2: classify every candidate into exactly one of SAFE/CAUTION/DANGER using the table criteria; cross-check git log --since='7 days ago' -- <resource file> for forced DANGER escalation; consult references/lenses/reliability.md for additive reversible reliability candidates
- Step 3: for each SAFE item — re-confirm signal currency, run terraform plan -target=<resource> as pre-change baseline, dispatch Task(subagent_type: ywc-cloud-engineer) scoped to the single resource with instructions to make the minimal .tf edit and run terraform plan (never apply), diff post-change plan against baseline, then commit with the exact required message shape (one commit per item, no batching)
- Step 4: for every CAUTION/DANGER item, write a report entry with rationale and a suggested next step (ywc-iac-author re-authoring or a direct capacity-commitment question to the user) without executing
- Step 5: hand off to ywc-verify-done with the full Infra Optimize Report in the specified Output Format, unless --skip-verify-done was passed by an upstream caller
- Create a feature branch for the SAFE-item commits and open a PR via gh pr create per the skill's stated downstream integration with ywc-create-pr
- Walk the Validation Checklist before declaring the pass complete

## Files touched
- docs/ywc-plans/pilot-artifacts/variant-ywc-infra-optimize-45-45.md (read only)

## Gates enforced
- Iron Law: cost/utilization data + per-item terraform plan blast-radius + reversibility/snapshot guarantee, all three required before any execution
- SAFE/CAUTION/DANGER tiering with multi-tier matches escalating to the highest tier
- Any stateful destroy/recreate (database, volume, bucket) is DANGER regardless of plan cleanliness or tool confidence
- Any resource touched by git log in the last 7 days is forced to DANGER
- Pre-change terraform plan baseline vs post-change plan diff must match only the intended delta before a SAFE item may be committed
- One SAFE item per commit — no batching, to preserve git bisectability
- terraform apply is never run at any point in this skill
- CAUTION and DANGER items are only escalated/reported, never auto-executed
- ywc-verify-done handoff mandatory unless --skip-verify-done explicitly passed by an upstream caller
- review/execute boundary preserved — this is a separate execution pass, not folded into an ywc-infra-review diagnosis pass

## Full narrative

I would open by announcing the skill exactly as the procedure requires: "I'm using the ywc-infra-optimize skill to improve existing infrastructure under a SAFE/CAUTION/DANGER change loop with per-item terraform plan verification."

Before touching anything, I would first confirm there is actually something to operate on. `ywc-agent-toolkit` is a skill/agent distribution repo — I would run `git ls-files '*.tf' '*.tfvars'` and `find . -iname "*.tf"` to check for a real Terraform tree. If, as expected, none exists here, I would not fabricate a scope; I would ask the user directly which repository or path holds the live Terraform state for the AWS/GCP/Azure/K8s infrastructure they want optimized, and confirm that credentials (AWS/GCP/Azure auth, a valid terraform backend, and a working `kubeconfig` context for any Kubernetes-managed resources) are available in the execution environment. Proceeding against a scope I invented, or against infrastructure I can't actually run `terraform plan`/`terraform init` against, would violate the Iron Law's first witness before I've even started.

Once a real scope is confirmed, Step 1 would run as follows. With no `--scope` given, I'd default to the full IaC tree, enumerating Terraform roots per cloud (e.g., `infra/aws`, `infra/gcp`, `infra/azure`, `infra/k8s`, or however the target repo organizes them) and any `terraform workspace list` entries per root. For each root I would run `terraform init -input=false` if needed, then `terraform plan -detailed-exitcode -out=driftplan.tfplan`, and `terraform show -json driftplan.tfplan` to get a structured diff — this is the drift-detection signal called for in Step 1. In parallel I would dispatch `Task(subagent_type: ywc-performance-engineer)` in read-only diagnostic mode, handing it the scoped Terraform tree plus explicit pointers to `references/lenses/cost.md` and `references/finops.md`, and asking it to produce a cost/utilization report: idle/orphaned resources (unattached EBS/GCP persistent disks/Azure managed disks, unused Elastic/static/public IPs, idle load balancers, orphaned snapshots), over-provisioned compute against observed p95 (CloudWatch, Cloud Monitoring, Azure Monitor, or `kubectl top`/metrics-server for K8s), and pricing-model mismatches (on-demand load that should move to reserved/spot). Per the fan-out contract, I would inject the §3.5 return-payload directive from `references/subagent-status-actions.md` so the dispatch returns `Status | 1-line summary | artifact path | Concerns (≤10 lines)` with full findings written to a file, and I would Read that returned artifact.

Step 2: I would classify every candidate from both signals into exactly one of SAFE / CAUTION / DANGER, escalating any multi-tier match to the highest tier. For each candidate I'd cross-check `git log --since="7 days ago" -- <resource file>` — anything touched in the last 7 days is forced to DANGER regardless of other signals. Any destroy/recreate against a stateful resource (database, volume, bucket) is DANGER outright. Reserved/spot adoption, moderate or ambiguous utilization, intentional-looking drift (manual hotfix), recent scaling history, or multi-AZ/read-replica additions all land in CAUTION. Additive, reversible reliability items — enabling backups/PITR on a store lacking them, adding a missing health/readiness check, raising an autoscaling floor to `min ≥ 2`, enabling deletion protection — are checked against `references/lenses/reliability.md` and, if genuinely additive with no recreation in the plan, classified SAFE.

Step 3, for each SAFE item, strictly one at a time and in order: (a) re-confirm the signal is still current rather than reusing the Step 1 snapshot — re-check the specific resource's utilization or config gap immediately before acting; (b) capture a pre-change baseline with `terraform plan -target=<resource_address> -out=pre.tfplan`; (c) dispatch `Task(subagent_type: ywc-cloud-engineer)` scoped to that single resource, carrying the classification rationale and an instruction to make the minimal `.tf` edit — e.g., delete an idle `resource "aws_eip" "unused_1" {...}` block, change `instance_type = "m5.4xlarge"` to `m5.large`, add a spot/reserved configuration block, or add a missing backup/health-check/autoscaling-floor/deletion-protection setting — and run `terraform plan -target=<resource_address> -out=post.tfplan`, never `terraform apply`; (d) diff `terraform show -json post.tfplan` against the pre-change baseline to confirm only the intended resource changed with zero unexpected destroys elsewhere — if the diff is wider than expected, I would re-classify the item to CAUTION and skip it rather than force it through; (e) commit with the exact required shape — `chore(infra): remove idle <resource> (cost lens)`, `fix(infra): right-size <resource> (cost lens, <from> → <to>)`, `fix(infra): remediate drift on <resource>`, or `fix(infra): enable <backup/health-check/autoscaling-floor> on <resource> (reliability lens)` — one item per commit, never batched, so the result stays bisectable with `git bisect` if a regression surfaces later.

Step 4: every CAUTION and DANGER item gets a report entry with rationale and a concrete next step — routing to `ywc-iac-author` for a reviewed re-authoring, or a direct question back to the user for anything requiring a reserved/spot capacity commitment — and none of them get executed in this pass, no matter how clean an individual `terraform plan` looks.

Step 5: I would hand off to `ywc-verify-done` with the full Infra Optimize Report using the specified template (scope, drift detected, cost/utilization signal source, executed SAFE items with commit shapes, escalated CAUTION/DANGER items with rationale, per-item `terraform plan` verification results, and an order-of-magnitude estimated monthly cost delta). Per the skill's Integration section, the optimization work would ship as its own branch and PR via `ywc-create-pr` / `gh pr create` rather than being folded into any other change.

Before declaring the pass complete I would walk the Validation Checklist verbatim: drift detection ran against the declared scope; the cost/utilization signal was gathered via `ywc-performance-engineer` before classification; every candidate landed in exactly one tier; every SAFE item has both a pre-change and post-change plan whose diff matched only the intended delta; reliability-hardening candidates were classified alongside cost/drift candidates, not skipped; every SAFE execution is its own commit; no CAUTION or DANGER item was executed; no `terraform apply` ran anywhere; and the final `ywc-verify-done` block uses canonical PASS/FAIL wording.

Throughout, I would hold the line against the specific rationalizations the procedure calls out: I would not batch SAFE commits for review convenience, would not treat the cost tool's "idle" flag alone as sufficient without the `terraform plan` blast-radius witness, would not auto-execute a CAUTION item because it "looks low-risk," would not treat a clean plan on a stateful destroy as safety, and would not execute anything while still inside an `ywc-infra-review` diagnostic pass — remediation is always a separate pass from diagnosis.
